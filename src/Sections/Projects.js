import React, { useEffect } from 'react';

export default function Projects() {
	useEffect(() => {
		window.scrollTo(0, 0);
	});

	return (
		<div className="section">
			<div>Coming soon ...</div>
		</div>
	);
}
